const axios = require("axios");

async function test() {
  console.log("Sending test message...");

  await axios.post(process.env.WEBHOOK_URL, {
    content: "🧪 TEST MESSAGE — If you see this, Discord works"
  });

  console.log("Sent!");
}

test();
